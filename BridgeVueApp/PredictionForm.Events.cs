using System;

namespace BridgeVueApp
{
    public partial class PredictionForm
    {
        // Required because the Designer has: Load += PredictionForm_Load;
        private void PredictionForm_Load(object? sender, EventArgs e)
        {
            // no-op: your constructor/Shown already does initialization
        }
    }
}

